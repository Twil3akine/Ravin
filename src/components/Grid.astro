---
---

<ul class="ravin-grid">
  <slot />
</ul>

<style>
  .ravin-grid {
    display: grid;
    /* 画面幅に合わせて自動で列数を調整し、最小幅を240pxに維持します */
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
    list-style: none;
    padding: 0;
    margin: 1.5rem 0;
  }
</style>
